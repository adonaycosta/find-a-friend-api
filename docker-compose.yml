services:
  postgres: 
      image: bitnami/postgresql
      container_name: postgres-db
      ports: 
        - 5432:5432
      environment:
        - POSTGRES_USERNAME=docker
        - POSTGRES_PASSWORD=docker
        - POSTGRES_DATABASE=findfriendapi

